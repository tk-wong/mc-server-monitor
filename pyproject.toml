[project]
name = "mc-server-auto-shutdown"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "google-cloud-compute>=1.43.0",
    "mcstatus>=12.2.1",
    "requests>=2.32.5",
]
